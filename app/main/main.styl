body { padding-top: 70px; }

.ng-cloak, .x-ng-cloak {
  display: none !important;
}

.options
    position absolute
    bottom 10px
    right 10px

.spaced
    margin-left 10px

.dropbox
    border 1px dashed
    padding 7px
    text-align center
    color gray

.smaller
    font-size small

.newdoc
  text-align center
  padding-left 10px

@media (max-width: 750px) {
  div.mobilerow {
    margin-left: 0px;
    margin-right: 0px;
  }
}
